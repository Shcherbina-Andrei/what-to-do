export enum AppRoute {
  Main = '/',
  SearchResult = '/search-results'
}

export enum TaskMode {
  Default = 'DEFAULT',
  Editing = 'EDITING',
  Details = 'DETAILS'
}

export enum NameSpace {
  Tasks = 'TASKS'
}

